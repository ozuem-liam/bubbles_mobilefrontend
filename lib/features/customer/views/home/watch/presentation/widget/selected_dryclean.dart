import 'package:flutter/material.dart';

class SelectedDryClean extends StatelessWidget {
  const SelectedDryClean({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
